% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_adapters.R
\name{predict_adapter.pprf_classification}
\alias{predict_adapter.pprf_classification}
\title{Predict using a fitted ppforest2 model}
\usage{
\method{predict_adapter}{pprf_classification}(model, newdata, ...)
}
\arguments{
\item{model}{A fitted \code{pprf_classification} object.}

\item{newdata}{A data frame of new observations to predict on.}

\item{...}{Additional arguments passed to \code{predict()}.}
}
\value{
A list containing \code{class} (predicted labels) and \code{probs} (probabilities).
}
\description{
Adapter function to generate standardized predictions from a ppforest2 model.

\strong{Developer Note:} During development, an issue was observed with
\code{ppforest2} v0.1.3 when training data is contiguous by class but
ordered so that the lowest class does not occur first (e.g.,
\code{Class 3, Class 3, Class 1, Class 1}). In this case, the underlying
C++ code can produce \code{Grouping::init: partition must be rooted at row 0}.
\code{classbound} does not reorder or otherwise modify the training data
specifically to avoid this behavior. If a future version of \code{ppforest2}
changes this behavior, no corresponding change to \code{classbound} is
expected to be necessary.
}
\keyword{internal}
